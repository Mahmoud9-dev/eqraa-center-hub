# تصميم الواجهة الرئيسية المحدثة لمركز إقرا

## 1. الهيكل العام للواجهة الرئيسية

```
┌─────────────────────────────────────────┐
│              مركز إقرا                 │
├─────────────────────────────────────────┤
│                                         │
│  [الإدارة]  [القرآن]  [التجويد]        │
│                                         │
│  [التربوي]  [الحضور]  [الامتحانات]     │
│                                         │
│  [دروس شرعية]  [الجدول]  [المكتبة]     │
│                                         │
│  [الإعلانات]  [الإعدادات]              │
│                                         │
└─────────────────────────────────────────┘
```

## 2. تفصيل الأيقونات والوظائف

### الصف الأول (الأقسام الأساسية)

- **الإدارة** 👥: إدارة الطلاب، المشايخ، البيانات الأساسية
- **القرآن** 📖: قائمة المشايخ، الجلسات، تتبع الحفظ
- **التجويد** 🎯: دروس التجويد، القواعد، التقييم

### الصف الثاني (المتابعة والتقييم)

- **التربوي** 🌱: المواد التربوية، السلوك، المتابعة
- **الحضور** ⏰: حضور وانصراف الطلاب والمشايخ
- **الامتحانات** 📝: امتحانات القرآن، التجويد، التربوي

### الصف الثالث (المحتوى التعليمي)

- **دروس شرعية** 📚: عقيدة، فقه، سيرة، تفسير، حديث
- **الجدول** 📅: الجداول اليومية/الأسبوعية، الإشعارات
- **المكتبة** 📖: كتب PDF، مقاطع صوتية، مراجع

### الصف الرابع (الإعدادات والتواصل)

- **الإعلانات** 📢: إعلانات عامة، تنبيهات، إشعارات
- **الإعدادات** ⚙️: إعدادات الحساب، الصلاحيات، التفضيلات

## 3. تصميم مكون IndexUpdated.tsx

```typescript
import IconButton from "@/components/IconButton";
import PageHeader from "@/components/PageHeader";

const IndexUpdated = () => {
  const sections = [
    // الصف الأول - الأقسام الأساسية
    { to: "/admin", icon: "👥", label: "الإدارة" },
    { to: "/quran", icon: "📖", label: "القرآن" },
    { to: "/tajweed", icon: "🎯", label: "التجويد" },

    // الصف الثاني - المتابعة والتقييم
    { to: "/tarbawi", icon: "🌱", label: "التربوي" },
    { to: "/attendance", icon: "⏰", label: "الحضور" },
    { to: "/exams", icon: "📝", label: "الامتحانات" },

    // الصف الثالث - المحتوى التعليمي
    { to: "/sharia", icon: "📚", label: "دروس شرعية" },
    { to: "/schedule", icon: "📅", label: "الجدول" },
    { to: "/library", icon: "📖", label: "المكتبة" },

    // الصف الرابع - الإعدادات والتواصل
    { to: "/announcements", icon: "📢", label: "الإعلانات" },
    { to: "/settings", icon: "⚙️", label: "الإعدادات" },
  ];

  return (
    <div className="min-h-screen bg-background">
      <PageHeader title="مركز إقرا" showBack={false} />

      <main className="container mx-auto px-4 py-12">
        <div className="text-center mb-12">
          <h2 className="text-3xl font-bold text-foreground mb-3">
            مرحباً بك في نظام إدارة المركز القرآني
          </h2>
          <p className="text-xl text-muted-foreground">
            اختر القسم المناسب للبدء
          </p>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto">
          {sections.map((section) => (
            <IconButton
              key={section.to}
              to={section.to}
              icon={section.icon}
              label={section.label}
            />
          ))}
        </div>
      </main>

      <footer className="bg-card border-t border-border py-6 mt-16">
        <div className="container mx-auto px-4 text-center">
          <p className="text-muted-foreground">
            © {new Date().getFullYear()} مركز إقرا - جميع الحقوق محفوظة
          </p>
        </div>
      </footer>
    </div>
  );
};

export default IndexUpdated;
```

## 4. تصميم الأقسام الفرعية

### 4.1 قسم الإدارة المحسن

```typescript
// صفحة الإدارة مع تحسين عرض الطلاب
const AdminPage = () => {
  const tabs = [
    { value: "students", label: "الطلاب", icon: "👥" },
    { value: "teachers", label: "المشايخ", icon: "👨‍🏫" },
    { value: "attendance", label: "الحضور", icon: "⏰" },
    { value: "statistics", label: "الإحصائيات", icon: "📊" },
  ];

  return (
    <div className="min-h-screen bg-background">
      <PageHeader title="الإدارة" />
      <Tabs defaultValue="students">
        <TabsList className="grid w-full grid-cols-4">
          {tabs.map((tab) => (
            <TabsTrigger key={tab.value} value={tab.value}>
              {tab.icon} {tab.label}
            </TabsTrigger>
          ))}
        </TabsList>

        <TabsContent value="students">
          <StudentsList /> {/* عرض الطلاب مع المرحلة الدراسية */}
        </TabsContent>

        <TabsContent value="teachers">
          <TeachersList /> {/* عرض المشايخ المتخصصين */}
        </TabsContent>

        <TabsContent value="attendance">
          <AttendanceManagement /> {/* نظام الحضور والانصراف */}
        </TabsContent>

        <TabsContent value="statistics">
          <StatisticsDashboard /> {/* إحصائيات المركز */}
        </TabsContent>
      </Tabs>
    </div>
  );
};
```

### 4.2 قسم القرآن المنظم

```typescript
// صفحة القرآن مع قائمة المشايخ والجلسات المنظمة
const QuranPage = () => {
  const [selectedTeacher, setSelectedTeacher] = useState<string | null>(null);

  return (
    <div className="min-h-screen bg-background">
      <PageHeader title="القرآن الكريم" />

      {!selectedTeacher ? (
        // عرض قائمة المشايخ
        <div className="container mx-auto px-4 py-12">
          <h2 className="text-2xl font-bold mb-6">اختر الشيخ لعرض جلساته</h2>
          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            {teachers.map((teacher) => (
              <Card
                key={teacher.id}
                className="cursor-pointer hover:shadow-lg transition-shadow"
                onClick={() => setSelectedTeacher(teacher.id)}
              >
                <CardContent className="p-6">
                  <h3 className="font-bold text-lg mb-2">{teacher.name}</h3>
                  <p className="text-muted-foreground">
                    {teacher.specialization}
                  </p>
                  <p className="text-sm text-primary mt-2">
                    عدد الطلاب: {teacher.students?.length || 0}
                  </p>
                </CardContent>
              </Card>
            ))}
          </div>
        </div>
      ) : (
        // عرض جلسات الشيخ المحدد
        <TeacherSessions
          teacherId={selectedTeacher}
          onBack={() => setSelectedTeacher(null)}
        />
      )}
    </div>
  );
};
```

### 4.3 تصميم الجلسات المنظمة زمنياً

```typescript
// مكون عرض الجلسات المنظمة زمنياً
const TeacherSessions = ({ teacherId, onBack }) => {
  const [sessions, setSessions] = useState({
    recent: [], // الماضي القريب
    distant: [], // الماضي البعيد
    new: [], // الجديد
  });

  return (
    <div className="container mx-auto px-4 py-12">
      <Button onClick={onBack} className="mb-6">
        ← العودة لقائمة المشايخ
      </Button>

      <div className="grid grid-cols-1 lg:grid-cols-3 gap-6">
        {/* الماضي القريب */}
        <Card>
          <CardHeader>
            <CardTitle className="text-blue-600">الماضي القريب</CardTitle>
          </CardHeader>
          <CardContent>
            {sessions.recent.map((session) => (
              <SessionCard key={session.id} session={session} />
            ))}
          </CardContent>
        </Card>

        {/* الماضي البعيد */}
        <Card>
          <CardHeader>
            <CardTitle className="text-gray-600">الماضي البعيد</CardTitle>
          </CardHeader>
          <CardContent>
            {sessions.distant.map((session) => (
              <SessionCard key={session.id} session={session} />
            ))}
          </CardContent>
        </Card>

        {/* الجديد */}
        <Card>
          <CardHeader>
            <CardTitle className="text-green-600">جديد</CardTitle>
          </CardHeader>
          <CardContent>
            <AddSessionForm teacherId={teacherId} />
            {sessions.new.map((session) => (
              <SessionCard key={session.id} session={session} />
            ))}
          </CardContent>
        </Card>
      </div>
    </div>
  );
};
```

## 5. تصميم بطاقة الطالب المحسنة

```typescript
// بطاقة عرض الطالب مع جميع البيانات المطلوبة
const StudentCard = ({ student }) => {
  return (
    <Card className="border-r-4 border-r-primary">
      <CardContent className="p-6">
        <div className="flex justify-between items-start mb-4">
          <div>
            <h4 className="font-bold text-lg">{student.name}</h4>
            <p className="text-sm text-muted-foreground">
              العمر: {student.age} سنة
            </p>
            <p className="text-sm text-muted-foreground">
              المرحلة: {student.grade}
            </p>
          </div>
          <Badge
            variant={student.department === "quran" ? "default" : "secondary"}
          >
            {getDepartmentLabel(student.department)}
          </Badge>
        </div>

        {student.department === "quran" && (
          <div className="bg-primary/10 p-3 rounded-lg">
            <div className="flex justify-between items-center">
              <span className="text-sm font-medium">الأجزاء المحفوظة</span>
              <span className="text-xl font-bold text-primary">
                {student.partsMemorized}
              </span>
            </div>
            <p className="text-xs text-muted-foreground mt-1">
              التقدم الحالي: {student.currentProgress}
            </p>
          </div>
        )}

        <div className="mt-4 flex gap-2">
          <Button size="sm" variant="outline">
            عرض التفاصيل
          </Button>
          <Button size="sm" variant="outline">
            تسجيل حضور
          </Button>
        </div>
      </CardContent>
    </Card>
  );
};
```

## 6. تصميم نظام الحضور والانصراف

```typescript
// صفحة الحضور والانصراف
const AttendancePage = () => {
  const [activeTab, setActiveTab] = useState("students");

  return (
    <div className="min-h-screen bg-background">
      <PageHeader title="الحضور والانصراف" />

      <Tabs value={activeTab} onValueChange={setActiveTab}>
        <TabsList className="grid w-full grid-cols-2">
          <TabsTrigger value="students">حضور الطلاب</TabsTrigger>
          <TabsTrigger value="teachers">حضور المشايخ</TabsTrigger>
        </TabsList>

        <TabsContent value="students">
          <StudentAttendance />
        </TabsContent>

        <TabsContent value="teachers">
          <TeacherAttendance />
        </TabsContent>
      </Tabs>
    </div>
  );
};

// مكون حضور الطلاب
const StudentAttendance = () => {
  return (
    <div className="container mx-auto px-4 py-12">
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
        {/* تسجيل الحضور */}
        <Card>
          <CardHeader>
            <CardTitle>تسجيل حضور جديد</CardTitle>
          </CardHeader>
          <CardContent>
            <AttendanceForm userType="student" />
          </CardContent>
        </Card>

        {/* عرض الحضور اليومي */}
        <Card>
          <CardHeader>
            <CardTitle>الحضور اليومي</CardTitle>
          </CardHeader>
          <CardContent>
            <TodayAttendance userType="student" />
          </CardContent>
        </Card>
      </div>
    </div>
  );
};
```

## 7. تصميم نظام الامتحانات

```typescript
// صفحة الامتحانات
const ExamsPage = () => {
  const [selectedSubject, setSelectedSubject] = useState("all");

  return (
    <div className="min-h-screen bg-background">
      <PageHeader title="الامتحانات والتقييم" />

      <div className="container mx-auto px-4 py-12">
        <div className="mb-6">
          <Select value={selectedSubject} onValueChange={setSelectedSubject}>
            <SelectTrigger className="w-48">
              <SelectValue placeholder="اختر المادة" />
            </SelectTrigger>
            <SelectContent>
              <SelectItem value="all">جميع المواد</SelectItem>
              <SelectItem value="quran">القرآن</SelectItem>
              <SelectItem value="tajweed">التجويد</SelectItem>
              <SelectItem value="tarbawi">التربوي</SelectItem>
            </SelectContent>
          </Select>
        </div>

        <div className="grid grid-cols-1 lg:grid-cols-2 gap-8">
          <ExamsList subject={selectedSubject} />
          <ExamResults subject={selectedSubject} />
        </div>
      </div>
    </div>
  );
};
```

## 8. ملخص التصميم

### المبادئ الأساسية:

1. **البساطة:** واجهة نظيفة وسهلة الاستخدام
2. **التنظيم:** تقسيم منطقي للأقسام والوظائف
3. **التكيف:** يعمل على جميع أحجام الشاشات
4. **الوضوح:** أيقونات وعناوين واضحة ومفهومة

### التحسينات الرئيسية:

1. **عرض البيانات الشامل:** اسم الطالب، العمر، المرحلة الدراسية
2. **التصنيف المنطقي:** تقسيم الطلاب حسب الأقسام
3. **التنظيم الزمني:** جلسات منظمة (ماضي قريب/بعيد/جديد)
4. **نظام الحضور:** متابعة يومية للطلاب والمشايخ
5. **التقييم الشامل:** امتحانات لكل مادة مع النتائج

### تجربة المستخدم:

1. **التنقل السلس:** عودة سهلة بين الأقسام
2. **البحث الفعال:** إمكانية البحث السريع في القوائم
3. **الإشعارات الواضحة:** رسائل نجاح وأخطاء واضحة
4. **الأداء السريع:** تحميل سريع للصفحات والبيانات

هذا التصميم يغطي جميع المتطلبات الجديدة التي ذكرتها مع الحفاظ على البساطة والوضوح.
